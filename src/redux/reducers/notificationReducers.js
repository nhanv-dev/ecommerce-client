// import undoable from 'redux-undo';

const notificationReducers = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

// const undoableTodos = undoable(notificationReducers);

export default notificationReducers;
// import undoable from 'redux-undo';

const notification = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

// const undoableTodos = undoable(notification);

export default notification;
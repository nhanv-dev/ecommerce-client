// import undoable from 'redux-undo';

const chatReducers = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

// const undoableChat = undoable(chatReducers);

export default chatReducers;
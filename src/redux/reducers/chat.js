// import undoable from 'redux-undo';

const chat = (state = [], action) => {
    switch (action.type) {
        default:
            return state
    }
}

// const undoableChat = undoable(chat);

export default chat;